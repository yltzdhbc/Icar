<launch>

  <!-- 启动hector -->
  <include file="$(find icar_slam)/launch/hector.launch"/>

  <!-- 启动rviz-->
  <node pkg="rviz" type="rviz" name="rviz" args="-d $(find icar_slam)/rviz/slam_hector.rviz"/>

</launch>